# Python
__pycache__/
*.pyc

# 采集数据 TSV 随仓库分发（2026-08-30 用户决策采集数据入仓；2026-09-06 错误成本数据配套全发布）
# ——旧版 *.tsv 忽略规则曾长期阻止其入库，2026-09-14 修复并补齐（git add 后须以 git ls-files 核验）

# 生成产物 —— 2026-09-22 用户决策: 产物**不由 git 管理、不发布**
#   产物七件 ~20MB, 每次刷新整体换新(历史无参考价值) → 只在本机 OneDrive `产物/` 保留最新一份
#   （下游 calc-schema 等直接读该目录）。主仓库只放代码 + 采集数据 + README。
#   旧产物(09-06~09-18 入库的 当量-* / keystroke_model.pt，~21MB)已于 2026-09-21 用
#   git filter-branch 自**全部历史**剔除(.git 25MB→2.6MB)。同日试过的 Release 附件与
#   独立产物仓库两条通道亦于 09-22 全部废弃。
#   本规则为安全网: sync_github.py 清单不含 产物/, 万一误拷也不会入库。
产物/
