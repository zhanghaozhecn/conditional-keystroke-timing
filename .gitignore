# Python
__pycache__/
*.pyc

# 采集数据 TSV 随仓库分发（2026-08-30 用户决策采集数据入仓；2026-09-06 错误成本数据配套全发布）
# ——旧版 *.tsv 忽略规则曾长期阻止其入库，2026-09-14 修复并补齐（git add 后须以 git ls-files 核验）

# 生成产物 —— 2026-09-21 用户决策: 产物**不进 git 历史**
#   理由: 七件产物 ~20MB, 每次数据刷新整体换新(历史产物无参考价值); 入库会让 .git 随刷新
#         线性膨胀(至 09-21 已达 21MB, 而代码+数据仅 ~1.5MB)。旧 09-18 "最终快照随仓库分发"
#         决策已被推翻, 且旧名产物(当量-*.txt / keystroke_model.pt)已自**全部历史**剔除
#         (git filter-branch, 2026-09-21)。
#   但用户仍需一份最新产物作参考 → 改由 **GitHub Release `products-latest`** 承担: 每次刷新
#         delete + recreate 该 release(tag 名固定, 永远只有一份最新), 附件即七件产物;
#         取用: `gh release download products-latest` 或 release 页面直接下载。
#   本规则为安全网 —— sync_github.py 复制阶段不含 产物/, 万一误拷也不会入库。
产物/
